# GitHub Actions config template for Gmail SMTP + Firestore (WIF)
# Register non-secret values as GitHub Actions Variables and credentials as
# GitHub Actions Secrets. The repository does not include an automatic
# registration script.
#
# Variables:
#   STATE_BACKEND, GCP_PROJECT_ID, FIRESTORE_DATABASE,
#   FIRESTORE_COLLECTION_PREFIX, NOTIFY_RETRY_*, monitoring targets,
#   NOTIFIER, SMTP_HOST, SMTP_PORT, SMTP_USE_TLS, email templates, WIF values
# Secrets:
#   SMTP_FROM, SMTP_TO, SMTP_USER, SMTP_PASS

# Runtime / backend
STATE_BACKEND=firestore
GCP_PROJECT_ID=your-gcp-project-id
FIRESTORE_DATABASE=(default)
FIRESTORE_COLLECTION_PREFIX=

# Retry (in-run exponential backoff)
NOTIFY_RETRY_MAX_RETRIES=2
NOTIFY_RETRY_INITIAL_DELAY_SECONDS=60

# Monitoring target
TARGET_NAME=監視対象名
TARGET_BANGUMI_ID=12345

# Notification backend
NOTIFIER=email

# Gmail SMTP (use Google App Password, not your normal Google account password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USE_TLS=true
SMTP_FROM=your-gmail-address@gmail.com
SMTP_TO=destination@example.com
SMTP_USER=your-gmail-address@gmail.com
SMTP_PASS=your-16-char-app-password

# Optional templates
EMAIL_SUBJECT_TEMPLATE=[ACM] {title}
EMAIL_BODY_TEMPLATE={title}\n\n{message}

# GitHub OIDC -> GCP WIF
GCP_WORKLOAD_IDENTITY_PROVIDER=projects/123456789012/locations/global/workloadIdentityPools/github-pool/providers/github-provider
GCP_SERVICE_ACCOUNT=animator-credit-monitor@your-gcp-project-id.iam.gserviceaccount.com
